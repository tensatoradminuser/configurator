using System;
using System.Windows.Forms;

namespace ABL_Parser
{
    static class Program
    {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new Form1());


        }
    }
}

// 10.53.0.1
// 10.53.2.15
// us-epicapp2.internal.tensator.com
// tracert 10.53.2.15

// System.Convert.ToDecimal(

//  CountryPalletTypes var_palletlookup x


//Refresh.DynamicList("cmb_tapeendselection");
//Refresh.DynamicList("cmb_webbinglength");

// Inputs.xxxx.Invisible = true:false;
// FieldInvisible('xxxx', true:false)

// replacecutref = REPLACE(labelwebbingfinish, "cut", "+");
// replacecutref = labelwebbingfinish.Replace("cut", "+");
// revisedwebdesc = TRIM(replacecutref.Entry(0, "+"));
// revisedwebdesc = replacecutref.Trim('+');

// Inputs.var_custsku.Value = Substring(custskucalc, 1, custskucalc.Length - 1);
// string custskucalc = "";
// Inputs.var_custsku.Value = custskucalc.Substring(1, custskucalc.Length - 1);

// PageLeaveFunctions.SetNextInputPage(1);
// PageLeaveFunctions.SetPrevInputPage(6);

// UDMethods.ConvertToDecimal(

